import 'package:flutter/material.dart';

class AppColor {
  static const Color background = Color(0xFF121212); // глубокий тёмный фон
  static const Color surface = Color(0xFF1E1E1E);    // панели, карточки
  static const Color primary = Color(0xFF00C853);    // яркий зелёный (под выделение)
  static const Color secondary = Color(0xFF66FFA6);  // светлее для акцентов
  static const Color border = Color(0xFF2C2C2C);     // для границ
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.grey;
}
